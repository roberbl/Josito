import { signOutAction } from '@/app/auth/actions';
import { requireUser } from '@/lib/auth';
export default async function Perfil(){const {profile}=await requireUser();return <div className="mx-auto max-w-xl space-y-5"><section className="card"><p className="text-sm font-black uppercase text-brand-700">Mi perfil</p><h1 className="text-3xl font-black">{profile?.displayName}</h1><p className="text-slate-600">{profile?.email}</p></section><section className="grid gap-3"><div className="card flex justify-between"><span className="font-bold">Rol</span><span className="status-pill bg-slate-100 text-slate-700">{profile?.role}</span></div><div className="card flex justify-between"><span className="font-bold">Estado</span><span className="status-pill bg-brand-50 text-brand-700">{profile?.participantStatus}</span></div><div className="card flex justify-between"><span className="font-bold">Equipo</span><span>{profile?.teamName ?? 'Sin equipo'}</span></div>{profile?.participantStatus==='pending'?<p className="rounded-3xl bg-amber-50 p-4 text-sm font-bold text-amber-800">Tu cuenta está pendiente de activación por el administrador.</p>:null}{profile?.participantStatus==='blocked'?<p className="rounded-3xl bg-red-50 p-4 text-sm font-bold text-red-700">Tu cuenta está bloqueada.</p>:null}</section><form action={signOutAction}><button className="btn-secondary w-full">Cerrar sesión</button></form></div>}
